# Temporary Build Artifacts

This directory is reserved for Quarto/Jupyter scratch files and intermediate render artifacts, such as `.quarto_ipynb`, `.tex`, `.aux`, `.log`, `.out`, and `.toc` files.

The source notebooks live in `../quarto/`, HTML previews live in `../html/`, and final PDFs live in `../output/`.
